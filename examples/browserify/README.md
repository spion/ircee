# ircee browserify example

To try it out, run the commands:

    npm install
    browserify client.js -o bundle.js
    node server.js

Then point the browser to http://localhost:8080
and open up the console to see all the irc events 
happening.

